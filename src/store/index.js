/* eslint-disable */
import { createStore } from "vuex";

export default createStore({
  /* data 역할 */
  state: {
    sharedData: "This is shared data",
  },
  /* computed 역할 */
  getters: {},

  mutations: {},
  actions: {},
  modules: {},
});
